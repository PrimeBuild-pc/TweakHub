<Window x:Class="TweakHub.Views.LoadingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.modernwpf.com/2019"
        Title="TweakHub"
        Width="400"
        Height="300"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        Icon="../ico.ico">
    
    <Border Background="{DynamicResource SystemControlBackgroundChromeMediumBrush}"
            CornerRadius="12"
            BorderBrush="{DynamicResource SystemControlBorderBaseLowBrush}"
            BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect Color="Black" Opacity="0.3" ShadowDepth="5" BlurRadius="15"/>
        </Border.Effect>
        
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <!-- Logo/Icon Area -->
            <Viewbox Grid.Row="1" Width="80" Height="80" Margin="0,20,0,0">
                <Border Width="80" Height="80" CornerRadius="16" Background="#1A1A1A"
                        BorderBrush="{StaticResource AccentBrush}" BorderThickness="3">
                    <TextBlock Text="TH" FontSize="28" FontWeight="Bold"
                               Foreground="{StaticResource AccentBrush}" HorizontalAlignment="Center"
                               VerticalAlignment="Center"/>
                </Border>
            </Viewbox>
            
            <!-- App Name -->
            <TextBlock Grid.Row="2" Text="TweakHub" 
                       FontSize="28" FontWeight="Light"
                       HorizontalAlignment="Center"
                       Margin="0,16,0,8"
                       Foreground="{DynamicResource SystemControlForegroundBaseHighBrush}"/>
            
            <!-- Loading Animation and Status -->
            <StackPanel Grid.Row="3" Orientation="Vertical" 
                        HorizontalAlignment="Center" Margin="0,20,0,40">
                
                <!-- Loading Spinner -->
                <Border Style="{StaticResource LoadingSpinnerStyle}" 
                        HorizontalAlignment="Center" Margin="0,0,0,16"/>
                
                <!-- Loading Text -->
                <TextBlock x:Name="LoadingText" 
                           Text="Initializing system analysis..."
                           FontSize="14"
                           HorizontalAlignment="Center"
                           Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}"/>
                
                <!-- Progress Bar -->
                <ProgressBar x:Name="LoadingProgress"
                             Width="200"
                             Height="4"
                             Margin="0,12,0,0"
                             Background="{DynamicResource SystemControlBackgroundBaseLowBrush}"
                             Foreground="{StaticResource AccentBrush}"
                             Value="0"
                             Maximum="100"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
